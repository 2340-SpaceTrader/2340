<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.gatech.cs2340.spacetrader">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_descriptor"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.AddPlayerActivity" />
        <activity android:name=".views.PlayingActivity"/>
        <activity android:name=".views.MarketActivity"/>
        <activity android:name=".views.BuyActivity"/>
        <activity android:name=".views.SellActivity"/>
        <activity android:name=".views.TravelActivity"/>
        <activity android:name=".views.MainActivity"/>
        <activity android:name=".views.RegisterActivity"/>
        <activity android:name=".views.SignInActivity"/>
    </application>

</manifest>